/**
 * IR Tab Component
 *
 * Debug tab showing intermediate representation details.
 */

import { observer } from 'mobx-react-lite';

export const IRTab = observer(function IRTab() {
  return (
    <div style={{ padding: '1rem' }}>
      <p>IR Tab not yet implemented</p>
    </div>
  );
});
