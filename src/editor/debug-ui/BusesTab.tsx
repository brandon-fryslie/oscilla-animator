/**
 * Buses Tab Component
 *
 * Debug tab showing bus (BusBlock) information.
 */

import { observer } from 'mobx-react-lite';

export const BusesTab = observer(function BusesTab() {
  return (
    <div style={{ padding: '1rem' }}>
      <p>Buses Tab not yet implemented</p>
    </div>
  );
});
