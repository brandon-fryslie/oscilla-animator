# Field Runtime Red Flags - Sprint 1 Plan

**Generated:** 2025-12-28
**Authoritative Plan:** `/plans/field-runtime-plan-v2.md`
**Implementation Checklist:** `/plans/field-runtime-implementation-checklist.md`

---

## Summary

This sprint fixes field runtime red flags by:
1. Making failures explicit (hard errors, not silent fallbacks)
2. Having the compiler wire default sources into field IR
3. Removing legacy params code after the new system works

See authoritative plan files for full details.

---

## Priority Order (to avoid churn)

| P | Item | Description |
|---|------|-------------|
| P0 | Hard error on signal fallback | `evalSig()` throws, not returns 0 |
| P0.5 | No silent default source fallback | Missing defaults = compile error |
| P1 | Compiler default source handling | Wire inputs → FieldExprConst/BroadcastSig |
| P2 | Remove legacy params code | Delete `readParamNumber`, `params` from types |
| P3 | Update audit document | Mark resolved, document new model |

---

## Validation (DevTools)

- [ ] Patch with JitterVec2 using only default inputs
- [ ] Patch with Vec2Rotate using only default inputs
- [ ] Patch with Vec2Scale using only default inputs
- [ ] Confirm no silent 0s, no missing input warnings
