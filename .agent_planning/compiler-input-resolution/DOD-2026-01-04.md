# Definition of Done: Compiler Input Resolution

**Generated:** 2026-01-04

## Overall Acceptance Criteria

- [ ] All "Unresolved input" errors eliminated for GridDomain, FieldStringToColor, RenderInstances2D
- [ ] All "must use outputsById pattern" errors eliminated for InfiniteTimeRoot, FiniteTimeRoot
- [ ] `just typecheck` passes with no errors
- [ ] `just test` passes with no failures
- [ ] Patch containing these blocks compiles successfully in browser

## Per-Block Criteria

### InfiniteTimeRoot
- [ ] Returns `outputsById: { systemTime, phase, pulse, energy }`
- [ ] Returns `outputs: []` (empty)
- [ ] No "must use outputsById pattern" error

### FiniteTimeRoot
- [ ] Returns `outputsById: { systemTime, phase, pulse, energy }`
- [ ] Returns `outputs: []` (empty)
- [ ] No "must use outputsById pattern" error

### GridDomain
- [ ] Function signature: `({ ctx, inputs, inputsById, config })`
- [ ] Uses `inputsById?.rows ?? inputs[0]` pattern for all 5 inputs
- [ ] Returns `outputsById: { domain, pos0 }`
- [ ] No "Unresolved input" errors

### FieldStringToColor
- [ ] Uses `inputsById?.strings ?? inputs[0]`
- [ ] Returns `outputsById: { colors: ... }`
- [ ] No "Unresolved input" errors

### RenderInstances2D
- [ ] All 7 inputs handled: domain, positions, radius, color, opacity, glow, glowIntensity
- [ ] Uses `inputsById` pattern (no array destructuring)
- [ ] No "Unresolved input" errors

## Not Required (Out of Scope)

- FieldFromExpression IR lowering (intentionally disabled)
- Oscillator changes (already correct)
